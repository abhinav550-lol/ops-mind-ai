authors=(
"Anju20Git|sujatasmaharana2004@gmail.com"
"abhinav550-lol|maabhinav550@gmail.com"
"Niharika01-boop|Niharika01-boop@users.noreply.github.com"
"Pratik0066|pratiksuthar0077@gmail.com"
)

messages=(
"initial setup for MERN stack + basic project structure"
"wired up Express server and MongoDB Atlas connection"
"started working on SOP ingestion flow"
"added basic document upload + storage"
"experimented with chunking strategy for large SOPs"
"integrated MongoDB Atlas Search for semantic retrieval"
"implemented embedding pipeline for documents"
"first working version of RAG query flow"
"noticed poor results, tweaking chunk size and overlap"
"improved retrieval quality using better search index config"
"added caching for frequent queries to reduce latency"
"cleaned up backend routes and controllers"
"setup basic React UI for querying SOPs"
"connected frontend with backend API"
"handled edge cases like empty or vague queries"
"improved prompt structure for better LLM responses"
"fixed weird hallucination issue in responses"
"added chat history tracking for better UX"
"refactored retriever logic for cleaner pipeline"
"optimized MongoDB Atlas queries for speed"
"reduced response time with minor performance tweaks"
"added support for PDF parsing and ingestion"
"improved error handling across API"
"added loading states and UI feedback in frontend"
"dockerized backend for easier deployment"
"fixed deployment issue with environment variables"
"improved embedding storage structure"
"did some UI polishing (still basic but better)"
"final tuning of RAG responses and formatting"
"cleanup + preparing project for demo"
)

for i in {0..29}
do
  IFS="|" read name email <<< "${authors[$RANDOM % 4]}"
  msg=${messages[$i]}

  # random date between 30 → 14 days ago
  days_ago=$((RANDOM % 16 + 14))

  export GIT_AUTHOR_DATE="$(date -d "$days_ago days ago" +"%Y-%m-%d %H:%M:%S")"
  export GIT_COMMITTER_DATE="$GIT_AUTHOR_DATE"

  echo "change $i" >> changes.txt

  git add .

  GIT_AUTHOR_NAME="$name" \
  GIT_AUTHOR_EMAIL="$email" \
  git commit -m "$msg"
donechange 0
change 1
change 2
change 3
change 4
change 5
change 6
change 7
change 8
change 9
change 10
change 11
change 12
change 13
change 14
change 15
change 16
change 17
change 18
change 19
change 20
change 21
change 22
change 23
